

def run(steps, runInfo):
	#first check if there are any args missing 
	#if so run the dialogue; if not do nothing
	#if you have to run the dialogue provide the user with the opportunity to quit 
	print "running interactive UI"
	quit = False
	return quit, steps, runInfo
#do stuff